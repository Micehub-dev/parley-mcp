# Risk Register

| ID | Risk | Probability | Impact | Mitigation | Owner | Review Date |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | CLI adapter behavior drift breaks the normalized participant contract | Medium | High | keep adapters narrow, validate shared structured output, keep runtime guardrails on by default, maintain service and stdio coverage, and rerun `npm run smoke:real` before release review | Micehub-dev core runtime maintainers | 2026-06-04 |
| R2 | Replay boundaries remain subtle after partial filesystem persistence failures | Medium | High | keep `storage_failure` explicit, preserve diagnostics outside session state, distinguish `artifact_invalid`, `artifact_unreadable`, and `directory_unreadable` on read paths, and preserve operator guidance in release docs | Micehub-dev core runtime maintainers | 2026-06-04 |
| R3 | Heuristic rolling summaries may still promote noisy topic memory | Medium | Medium | keep `rollingSummary` and `conclusion` contract-stable, deduplicate repeated questions and action items, avoid consensus carryover from `undecided`, and review promoted outputs during release smoke | Micehub-dev maintainers | 2026-06-04 |
| R4 | Packaging or UI work resumes before the Sprint 12 production-readiness bar is stable | Medium | Medium | keep packaging and UI downstream of the current stdio-first support statement, and require Sprint 12 evidence review and smoke discipline before expanding surfaces | Micehub-dev maintainers | 2026-06-04 |
| R5 | Windows participant launchers remain environment-sensitive under `spawn`, especially when only `gemini.ps1` is available instead of the npm `gemini.cmd` shim | Medium | Medium | prefer the npm-installed `gemini.cmd` shim on Windows, keep `PARLEY_*_COMMAND` and `PARLEY_*_ARGS_JSON` available as fallback overrides, verify launcher behavior in `npm run smoke:real`, keep the Windows Codex Desktop checklist aligned with the same launcher facts, and rerun smoke when a timeout suggests launcher or auth drift | Micehub-dev release maintainer | 2026-06-04 |
| R6 | Diagnostics tooling could leak raw subprocess detail if redaction defaults regress during hardening work | Low | Medium | keep redacted diagnostics as the default MCP view, treat `detailLevel: "full"` as local-only debugging, and verify both paths in service and stdio tests | Micehub-dev core runtime maintainers | 2026-06-04 |
| R7 | The current support position could be overstated if CI evidence or a single Windows smoke pass is confused with broader real-environment validation | Medium | Medium | keep `docs/test-matrix.md` explicit about automated versus real-environment evidence, widen support wording only from exercised additional environments, and publish caveats in release notes until broader evidence exists | Micehub-dev release maintainer | 2026-06-04 |
| R8 | Gemini may remain contract-valid but low-value if real CLI responses drift toward generic plain text, partial shapes, or the default next step | Medium | Medium | keep adapter normalization narrow, add regression coverage for fenced JSON, labeled plain text, partial JSON, and low-value fallback patterns, keep the stronger anti-fallback prompt wording in place, treat the default next step as fallback-grade during review, and inspect smoke artifacts before release signoff | Micehub-dev core runtime maintainers | 2026-06-04 |
| R9 | Release evidence can drift across smoke docs, acceptance docs, matrix docs, and support-boundary wording, causing stale or conflicting release claims | Medium | Medium | maintain one repeatable release-evidence workflow, prefer the generated `releaseEvidence` or emitted `.json` and `.md` artifacts from smoke, keep concise launcher facts in the generated artifact, refresh smoke and acceptance facts together, and update the matrix and runbook in the same release-review pass | Micehub-dev release maintainer | 2026-06-04 |
| R10 | Workstation-specific Gemini auth or runtime health can still turn a release smoke into a timeout even after automation and launcher hardening | Medium | High | rerun `npm run smoke:real` with preserved diagnostics when timeouts occur, verify Gemini CLI authentication and launcher health on the release workstation, and do not treat automation or historical smoke passes as substitutes for a clean current release smoke | Micehub-dev release maintainer | 2026-06-04 |
