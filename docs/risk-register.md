# Risk Register

| ID | Risk | Probability | Impact | Mitigation | Owner | Review Date |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | CLI adapter behavior drift breaks the normalized participant contract | Medium | High | keep adapters narrow, validate shared structured output, keep runtime guardrails on by default, maintain service and stdio coverage, and rerun `npm run smoke:real` before release review | Micehub-dev core runtime maintainers | 2026-05-07 |
| R2 | Replay boundaries remain subtle after partial filesystem persistence failures | Medium | High | keep `storage_failure` explicit, preserve diagnostics outside session state, distinguish `artifact_invalid`, `artifact_unreadable`, and `directory_unreadable` on read paths, and preserve operator guidance in release docs | Micehub-dev core runtime maintainers | 2026-05-07 |
| R3 | Heuristic rolling summaries may still promote noisy topic memory | Medium | Medium | keep `rollingSummary` and `conclusion` contract-stable, deduplicate repeated questions and action items, avoid consensus carryover from `undecided`, and review promoted outputs during release smoke | Micehub-dev maintainers | 2026-05-07 |
| R4 | Packaging or UI work resumes before the Sprint 10 production-use evidence bar is stable | Medium | Medium | keep packaging and UI downstream of the current stdio-first support statement, and require Sprint 10 evidence review and smoke discipline before expanding surfaces | Micehub-dev maintainers | 2026-05-07 |
| R5 | Windows participant launchers remain environment-sensitive under `spawn`, especially when only `gemini.ps1` is available instead of the npm `gemini.cmd` shim | Medium | Medium | prefer the npm-installed `gemini.cmd` shim on Windows, keep `PARLEY_*_COMMAND` and `PARLEY_*_ARGS_JSON` available as fallback overrides, verify launcher behavior in `npm run smoke:real`, and keep the Windows Codex Desktop checklist aligned with the same launcher facts | Micehub-dev release maintainer | 2026-05-07 |
| R6 | Diagnostics tooling could leak raw subprocess detail if redaction defaults regress during hardening work | Low | Medium | keep redacted diagnostics as the default MCP view, treat `detailLevel: "full"` as local-only debugging, and verify both paths in service and stdio tests | Micehub-dev core runtime maintainers | 2026-05-07 |
| R7 | The current support position could be overstated if Ubuntu CI evidence or a single Windows smoke pass is confused with broader real-environment validation | Medium | Medium | keep `docs/test-matrix.md` explicit about automated versus real-environment evidence, widen support wording only from exercised additional environments, and publish caveats in release notes until broader evidence exists | Micehub-dev release maintainer | 2026-05-07 |
| R8 | Gemini may remain contract-valid but low-value if real CLI responses drift toward generic plain text or partial shapes that normalization only partially recovers | Medium | Medium | keep adapter normalization narrow, add regression coverage for fenced JSON, labeled plain text, partial JSON, and newly observed low-value fallback patterns, keep the stronger anti-fallback prompt wording in place, and review the smoke-time `geminiUsefulness` field before release signoff | Micehub-dev core runtime maintainers | 2026-05-07 |
| R9 | Release evidence can drift across smoke docs, acceptance docs, matrix docs, and support-boundary wording, causing stale or conflicting release claims | Medium | Medium | maintain one repeatable release-evidence workflow, refresh smoke and acceptance facts together, use `docs/release-evidence-template.md` as the default note shape, and update the matrix and runbook in the same release-review pass | Micehub-dev release maintainer | 2026-05-07 |
