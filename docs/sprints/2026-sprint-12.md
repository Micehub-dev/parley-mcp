# Sprint 12 Brief

## Dates

- Planned window: 2026-05-22 to 2026-06-04
- Planning note: Sprint 11 established Windows CI parity, generated smoke evidence, and another clean Windows local smoke was exercised on 2026-03-16, but release readiness is still bottlenecked by stale evidence records, an overly permissive Gemini usefulness gate, and noisy release artifacts.

## Goal

- turn the current production-readiness outputs into a cleaner release-review baseline by aligning the evidence set to the latest exercised facts
- tighten the Gemini usefulness gate so low-value smoke passes fail honestly
- simplify generated release artifacts so maintainers can review and attach them without manual cleanup

## Status

- Planned as of 2026-03-16

## Why This Sprint Now

- The core MCP contract, storage hardening, diagnostics redaction, and Windows-plus-Ubuntu automation bar are already in place, so the next highest-leverage work is release trust rather than new product surface area.
- A fresh Windows local `npm run smoke:real` pass now exists on 2026-03-16, but the document set still needs an explicit cleanup pass so release review points at the latest exercised evidence instead of stale mixed notes.
- The current Gemini usefulness classifier can still mark a low-value response as `material` when the response keeps the default next step and generic filler, which weakens the production-readiness signal.
- The generated smoke evidence is useful but too verbose in human-facing form because launcher fields currently expand into full prompt payloads instead of concise launcher facts.

## Committed Scope

- [ ] Clean up the production-readiness evidence set so smoke, sprint, matrix, and release docs all point at the latest exercised facts
- [ ] Tighten the Gemini usefulness gate in smoke and release review without widening the shared `ParticipantResponse` contract
- [ ] Simplify generated release artifacts so they are concise, attachable, and directly reviewable by maintainers
- [ ] Keep the planned workspace-scoped file-reading MCP tool explicitly downstream of this production-readiness pass

## Stretch Scope

- [ ] Add one lightweight regression or release-gating check that fails clearly when smoke evidence fields or usefulness thresholds drift
- [ ] Record remote GitHub Actions evidence for the Windows lane in the same release-evidence workflow if it can be captured without broadening the sprint
- [ ] Capture one short packaging-readiness decision note only if the Sprint 12 production-readiness bar exits cleanly

## Explicit Non-Goals

- [x] no new end-user MCP tools, including the workspace-scoped file-reading surface, until this sprint closes cleanly
- [x] no UI, extension, plugin, or packaging implementation
- [x] no session-state or MCP contract expansion beyond additive production-readiness hardening
- [x] no database, vector-store, or hosted control-plane work
- [x] no broader macOS or Linux support claim without exercised evidence

## Exit Criteria

- [ ] The latest smoke, matrix, sprint, and release docs agree on the most recent exercised evidence and separate historical caveats from the current baseline clearly
- [ ] Gemini responses that still rely on generic filler or the default next step no longer pass the smoke usefulness bar as `material`
- [ ] Generated release evidence summarizes launcher provenance and review facts concisely enough to attach directly to release review without manual trimming
- [ ] `npm test`, `npm run lint`, `npm run typecheck`, `npm run build`, and `npm run smoke:real` are green when Sprint 12 closes
- [ ] The read-only workspace-scoped file-reading MCP tool remains deferred until the above production-readiness work is complete

## Dependencies

- Preserve the current stdio-first support posture and the Sprint 7 through Sprint 11 hardening behavior
- Keep diagnostics redaction, subprocess guardrails, and replay-boundary semantics unchanged unless a production-readiness gap clearly requires additive refinement
- Keep Gemini quality work confined to prompting, normalization, usefulness scoring, and release review rather than participant-specific session-state semantics
- Prefer concise release-review artifacts over larger generated payloads that require manual interpretation

## Owners

- PM: Micehub-dev maintainers
- Engineering: Micehub-dev core runtime maintainers
- Release captain: Micehub-dev release maintainer

## Task Tracker

### Task 1. Evidence Cleanup

- [ ] Update `docs/real-cli-smoke.md`, `docs/test-matrix.md`, `docs/release-checklist.md`, and sprint notes so they reflect the latest exercised smoke facts without treating older caveats as the current baseline
- [ ] Separate the 2026-03-13 timeout note from the latest clean Windows smoke result so release review reads the evidence in the correct order
- [ ] Keep the support boundary narrow and evidence-backed while the product remains Windows-first for real operator use
- [ ] Refresh PM planning docs so Sprint 12 priorities match the current production-readiness sequence

Delivered when:

- release review can identify the current exercised baseline without cross-checking multiple stale caveats
- historical smoke issues remain visible but no longer masquerade as the latest result
- the documentation set points at one coherent production-readiness story

Verification:

- doc review against the latest local smoke facts
- updated `docs/real-cli-smoke.md`
- updated `docs/test-matrix.md`
- updated `docs/release-checklist.md`
- updated `docs/project-operating-plan.md`

### Task 2. Gemini Usefulness Gate Tightening

- [ ] Convert the low-value Gemini pattern observed in the latest smoke path into targeted regression coverage
- [ ] Tighten usefulness scoring so generic filler plus the default next step no longer clears the release bar as `material`
- [ ] Keep the smoke-time release decision aligned to the stricter usefulness bar
- [ ] Preserve the shared response contract and avoid participant-specific session-state changes

Delivered when:

- release smoke distinguishes a truly useful Gemini response from a merely schema-valid one more honestly
- newly observed low-value patterns are covered by automated regression tests
- the release decision in smoke output is no longer optimistic about generic fallback responses

Verification:

- `npm test`
- `npm run smoke:real`
- targeted adapter and smoke-output assertions

### Task 3. Release Artifact Simplification

- [ ] Replace oversized human-facing launcher summaries with concise launcher facts that preserve provenance without embedding the full participant prompt
- [ ] Keep generated release-evidence `.json` and `.md` artifacts directly usable in release review
- [ ] Reduce ambiguous default fields where smoke can record a clearer fact automatically
- [ ] Add or refresh tests for the simplified artifact shape

Delivered when:

- generated smoke evidence is short enough to read and attach directly
- launcher provenance remains clear without copying the entire prompt payload into the release note
- maintainers spend less time cleaning up or trimming generated artifacts during release review

Verification:

- `npm test`
- `npm run smoke:real`
- updated `test/release-evidence.test.ts`
- review of generated Markdown output

## Outcome Notes

- Sprint 12 should remain a production-readiness sprint rather than a feature sprint.
- The requested sequence for this sprint is evidence cleanup first, usefulness gate tightening second, and release artifact simplification third.
- The read-only workspace-scoped file-reading MCP tool should stay next in line, but only after this release-signal cleanup pass exits cleanly.
- If Sprint 12 uncovers another real-smoke regression, treat that as a release blocker instead of reopening feature expansion.
